<span class="speech @Arrow">
    @ChildContent
</span>

@code {
    public enum Direction {
        bottom, top, left, right
    }
    [Parameter] public Direction Arrow {get; set;}
    [Parameter] public RenderFragment? ChildContent {get; set;}
}